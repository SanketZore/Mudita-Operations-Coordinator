{
  "name": "ops-coordinator-agents",
  "version": "1.0.0",
  "description": "Three connected LLM agents (Intake -> Planning -> Review) that turn a meeting transcript and company rules into a reviewed action plan.",
  "type": "module",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
